const C='gengrail-log-v24.11.14-market-dislocation';
const A=['./','./index.html','./manifest.json','./gengrail-theme.css','./gengrail-ebay.js','./gengrail-profit-engine.js','./profit-engine-diagnostic.js','./home-compact.css','./grail-hub.css','./grail-pre2002-safety.js','./grail-visual-gate.js','./grail-hub.js','./grail-selectable-stream-v1.js','./icon-192.png','./icon-512.png'];
self.addEventListener('install',e=>{e.waitUntil(caches.open(C).then(c=>c.addAll(A)));self.skipWaiting()});
self.addEventListener('activate',e=>{e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==C).map(k=>caches.delete(k)))));self.clients.claim()});
self.addEventListener('fetch',e=>{
 if(e.request.method!=='GET')return;
 const path=new URL(e.request.url).pathname;
 if(path.endsWith('/temporary-test-repo/')||path.endsWith('/temporary-test-repo/index.html')||path.endsWith('/index.html')){
  const req=new Request(e.request,{cache:'reload'});
  e.respondWith(fetch(req).then(r=>{const copy=r.clone();caches.open(C).then(c=>c.put(e.request,copy)).catch(()=>{});return r}).catch(()=>caches.match(e.request)));
  return;
 }
 if(path.endsWith('/grail-hub.js')){
  e.respondWith((async()=>{
   try{
    const [safety,gate,hub,selectable]=await Promise.all([
     fetch(new URL('./grail-pre2002-safety.js',self.location.href),{cache:'reload'}),
     fetch(new URL('./grail-visual-gate.js',self.location.href),{cache:'reload'}),
     fetch(new Request(e.request,{cache:'reload'})),
     fetch(new URL('./grail-selectable-stream-v1.js',self.location.href),{cache:'reload'})
    ]);
    if(!safety.ok||!gate.ok||!hub.ok||!selectable.ok)throw new Error('grail_bundle_fetch_failed');
    let s=await selectable.text();
    const lanes="const LANES=['Pokemon TCG Illustration Rare single card','Pokemon TCG Pokemon 151 single card','Pokemon TCG Special Illustration Rare single card','Pokemon TCG Paldean Fates single card','Pokemon TCG Galarian Gallery single card','Pokemon TCG Crown Zenith single card','Pokemon TCG Trainer Gallery single card','Pokemon TCG Brilliant Stars single card','Pokemon TCG Alternate Art single card','Pokemon TCG Evolving Skies single card','Pokemon TCG Secret Rare single card','Pokemon TCG Lost Origin single card','Pokemon TCG Hyper Rare single card','Pokemon TCG Silver Tempest single card','Pokemon TCG Gold Rare single card','Pokemon TCG Temporal Forces single card','Pokemon TCG Full Art single card','Pokemon TCG Twilight Masquerade single card','Pokemon TCG Illustration Rare single card','Pokemon TCG Stellar Crown single card','Pokemon TCG Special Illustration Rare single card','Pokemon TCG Surging Sparks single card','Pokemon TCG Character Rare single card','Pokemon TCG Prismatic Evolutions single card','Pokemon TCG Secret Illustration Rare single card','Pokemon TCG Journey Together single card','Pokemon TCG Ultra Rare single card','Pokemon TCG Destined Rivals single card','Pokemon TCG Illustration Rare single card','Pokemon TCG Black Bolt single card','Pokemon TCG Special Illustration Rare single card','Pokemon TCG White Flare single card'];";
    const economics="function economics(c,compItems){const comps=compItems.filter(x=>compMatches(c,x)&&String(x?.itemId||'')!==c.id);const rawPrices=comps.map(price).filter(x=>x>0).sort((a,b)=>a-b);if(rawPrices.length<2)return null;let prices=[],bestSpread=Infinity,bestMid=Infinity;for(let i=0;i<rawPrices.length;i++){let j=i;while(j+1<rawPrices.length&&rawPrices[j+1]-rawPrices[i]<=10)j++;const cluster=rawPrices.slice(i,j+1),spread=cluster[cluster.length-1]-cluster[0],mid=median(cluster);if(cluster.length>prices.length||(cluster.length===prices.length&&(spread<bestSpread||(spread===bestSpread&&mid<bestMid)))){prices=cluster;bestSpread=spread;bestMid=mid}}if(prices.length<2)return null;const med=median(prices),low=percentile(prices,.35),market=(med+low)/2,landed=c.ask+c.ship,outbound=0,pack=num(localStorage.getItem('gengrailOpp_oppPack'))||.30,feeRate=.135,fees=market*feeRate,profit=market-fees-pack-landed,roi=landed>0?profit/landed*100:0,margin=market>0?profit/market*100:0,confidence=Math.min(.94,.68+Math.min(.18,prices.length*.015));const maxLandedRoi=(market*(1-feeRate)-pack)/1.10,maxLandedMargin=market*(1-feeRate-.10)-pack,maxLanded=Math.max(0,Math.min(maxLandedRoi,maxLandedMargin)),maxBuy=Math.max(0,maxLanded-c.ship),pass=profit>0&&roi>=10&&margin>=10,preferred=profit>0&&roi>=15&&margin>=15,negotiable=!pass&&maxBuy>0&&maxBuy<c.ask&&maxBuy>=c.ask*.60,watch=!pass&&!negotiable&&market>landed,velocityScore=(Math.min(prices.length,8)*10)+(confidence*25)+(preferred?28:pass?20:negotiable?12:0)+(c.localDiscount||0)*1.8-Math.min(18,landed/20);return {...c,peerCount:prices.length,rawPeerCount:rawPrices.length,compWindow:5,compSpread:bestSpread,marketMedian:med,lowerBand:low,market,landed,outbound,pack,fees,profit,roi,margin,confidence,maxBuy,pass,preferred,negotiable,watch,velocityScore};}";
    const shortlist="function cheapShortlist(items,lane,maxPrice){const rows=[];for(const item of items){const c=identity(item,lane);if(!c||!c.id||c.ask<1.5)continue;const landed=c.ask+c.ship;if(landed>maxPrice)continue;c.discoveryLanded=landed;rows.push(c)}const groups=new Map();for(const c of rows){const key=[c.n,c.name,c.grader||'RAW',c.grade||''].join('|');if(!groups.has(key))groups.set(key,[]);groups.get(key).push(c)}const scored=[];for(const group of groups.values()){if(group.length<2)continue;const costs=group.map(x=>x.discoveryLanded).filter(x=>x>0).sort((a,b)=>a-b),med=median(costs);if(!med)continue;group.sort((a,b)=>a.discoveryLanded-b.discoveryLanded);const best=group[0],discount=(med-best.discoveryLanded)/med*100;if(discount>=8){best.localMedian=med;best.localDiscount=discount;best.localPeers=group.length;scored.push(best)}}scored.sort((a,b)=>(b.localDiscount||0)-(a.localDiscount||0)||(b.localPeers||0)-(a.localPeers||0)||a.discoveryLanded-b.discoveryLanded);if(scored.length>=2)return scored.slice(0,6);const chase=rows.filter(c=>/illustration rare|special illustration|sir\b|alternate art|alt art|galarian gallery|trainer gallery|secret rare|hyper rare|gold rare|ultra rare|character rare/i.test(c.title)).sort((a,b)=>a.discoveryLanded-b.discoveryLanded);const out=scored.slice(),seen=new Set(out.map(x=>x.query));for(const c of chase){if(seen.has(c.query))continue;seen.add(c.query);c.localMedian=0;c.localDiscount=0;c.localPeers=1;out.push(c);if(out.length>=6)break}return out.slice(0,6)}";
    const cardHtml="function cardHtml(x,i){const selected=S.selected.has(x.id),status=x.preferred?'VELOCITY BUY':x.pass?'BUY':x.negotiable?'NEGOTIATE':x.watch?'WATCH':'PASS',dis=x.localDiscount>0?` · ${x.localDiscount.toFixed(0)}% below local active median`:'';const action=x.pass?`<button class=\"select ${selected?'on':''}\" data-gsel-select=\"${esc(x.id)}\">${selected?'SELECTED ✓':'SELECT'}</button>`:x.negotiable?`<button disabled>MAX BUY ${money(x.maxBuy)}</button>`:'';return `<article class=\"gsel-card ${x.pass?'pass':''}\"><img src=\"${esc(x.image||'icon-192.png')}\" alt=\"\"><div><h3>${esc(x.title)}</h3><div class=\"gsel-card-sub\">#${i+1} · ${status} · ${x.peerCount} targeted comps · ${(x.confidence*100).toFixed(0)}% evidence confidence${dis} · ${esc(x.set)}</div><div class=\"gsel-grid\"><div><small>LANDED BUY</small><b>${money(x.landed)}</b></div><div><small>CONSERVATIVE MARKET</small><b>${money(x.market)}</b></div><div><small>PROJECTED NET</small><b class=\"gsel-good\">${money(x.profit)}</b></div><div><small>ROI / MARGIN</small><b>${x.roi.toFixed(0)}% / ${x.margin.toFixed(0)}%</b></div></div><div class=\"gsel-card-sub\">Comp median ${money(x.marketMedian)} · lower band ${money(x.lowerBand)}. Active asking prices are evidence, not sold prices.</div><div class=\"gsel-actions\">${x.url?`<a href=\"${esc(x.url)}\" target=\"_blank\" rel=\"noopener\">OPEN EBAY</a>`:''}${action}</div></div></article>`}";
    s=s.replace(/const PATCH_VERSION='[^']+';/,"const PATCH_VERSION='selectable-stream-v1.2.4';")
      .replace(/const LANES=\[[\s\S]*?\];/,lanes)
      .replace(/function economics\(c,compItems\)\{[\s\S]*?\}\nfunction cheapShortlist/,economics+'\nfunction cheapShortlist')
      .replace(/function cheapShortlist\(items,lane,maxPrice\)\{[\s\S]*?\}\nfunction rotateLanes/,shortlist+'\nfunction rotateLanes')
      .replace(/const n=6,start=/,'const n=8,start=')
      .replace('unique.slice(0,18)','unique.slice(0,24)')
      .replace(/function cardHtml\(x,i\)\{[\s\S]*?\}\nfunction render/,cardHtml+'\nfunction render')
      .replace('Find qualified buys first. You choose the basket.','SEED velocity: hunt live listings materially below their local active market.')
      .replace('Modern 2002+ rotating discovery → unique card shortlist → targeted comp search. Minimum selectable floor: 10% ROI and 10% net margin. 15%+ is preferred.','Market-dislocation discovery: group same-card live listings, prioritise the cheapest listing when it sits materially below its local active median, then run targeted comps. 10% ROI + 10% net margin remains the protected BUY floor.')
      .replace(/if\(e&&e\.profit>0&&e\.margin>=5\)rows\.push\(e\)/g,'if(e&&(e.pass||e.negotiable||e.watch))rows.push(e)')
      .replace(/rows\.sort\(\(a,b\)=>Number\(b\.pass\)-Number\(a\.pass\)\|\|Number\(b\.preferred\)-Number\(a\.preferred\)\|\|b\.profit-a\.profit\|\|b\.roi-a\.roi\)/g,'rows.sort((a,b)=>Number(b.preferred)-Number(a.preferred)||Number(b.pass)-Number(a.pass)||Number(b.negotiable)-Number(a.negotiable)||(b.velocityScore||0)-(a.velocityScore||0)||b.profit-a.profit)')
      .replace(/S\.rows=rows\.sort\(\(a,b\)=>Number\(b\.pass\)-Number\(a\.pass\)\|\|b\.profit-a\.profit\)\.slice\(0,20\)/g,'S.rows=rows.sort((a,b)=>Number(b.preferred)-Number(a.preferred)||Number(b.pass)-Number(a.pass)||Number(b.negotiable)-Number(a.negotiable)||(b.velocityScore||0)-(a.velocityScore||0)||b.profit-a.profit).slice(0,20)')
      .replace(/Discovery returned candidates but none produced 3\+ targeted comps with positive economics\./g,'Discovery found candidates, but none cleared the current BUY / NEGOTIATE / WATCH economics after targeted comping.')
      .replace(/No defensible targeted opportunities produced 3\+ comparable listings\./g,'No defensible targeted opportunities produced a usable targeted comp cluster.');
    const routePatch=`\n(function(){'use strict';if(window.__gengrailSelectableRouteV124)return;window.__gengrailSelectableRouteV124=true;document.addEventListener('click',function(e){const btn=e.target&&e.target.closest&&e.target.closest('#grailOpportunityAction');if(!btn)return;if(!(window.GengrailSelectableStream&&typeof window.GengrailSelectableStream.render==='function'))return;e.preventDefault();e.stopPropagation();setTimeout(function(){try{window.GengrailSelectableStream.render();}catch(err){console.error('Opportunity Stream render failed',err);}},0);},true);})();`;
    const body=(await safety.text())+'\n\n'+(await gate.text())+'\n\n'+(await hub.text())+'\n\n'+s+'\n\n'+routePatch;
    return new Response(body,{status:200,headers:{'content-type':'application/javascript;charset=UTF-8','cache-control':'no-store'}});
   }catch(err){return caches.match(e.request).then(r=>r||fetch(e.request))}
  })());
  return;
 }
 const forceReload=path.endsWith('/grail-pre2002-safety.js')||path.endsWith('/grail-visual-gate.js')||path.endsWith('/grail-selectable-stream-v1.js')||path.endsWith('/grail-hub.css')||path.endsWith('/profit-engine-diagnostic.js')||path.endsWith('/gengrail-theme.css');
 const req=forceReload?new Request(e.request,{cache:'reload'}):e.request;
 e.respondWith(fetch(req).then(r=>{const copy=r.clone();caches.open(C).then(c=>c.put(e.request,copy)).catch(()=>{});return r}).catch(()=>caches.match(e.request)));
});
